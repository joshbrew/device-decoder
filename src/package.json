{
  "name": "device-decoder",
  "version": "0.2.23",
  "description": "Customizable multithreaded BLE and USB (or other) device connecting, sensor decoding, and (threaded) output rendering",
  "main": "dist/device.frontend.js",
  "module": "dist/device.frontend.esm.js",
  "types": "dist/device.frontend.d.ts",
  "scripts": {
    "start": "tinybuild",
    "build": "tinybuild build",
    "serve": "tinybuild serve",
    "init": "node tinybuild/init.js",
    "concurrent": "concurrently \\'npm run python\\' \\'npm start\\'",
    "dev": "npm run pip && npm i --save-dev concurrently && npm i --save-dev nodemon && npm run concurrent",
    "startdev": "nodemon --exec \\'node tinybuild.js\\' -e ejs,js,ts,jsx,tsx,css,html,jpg,png,scss,txt,csv",
    "python": "python python/server.py",
    "pip": "pip install quart && pip install websockets",
    "pwa": "npm i workbox-cli && workbox generateSW node_server/pwa/workbox-config.js && npm run build && npm start"
  },
  "keywords": [
    "esbuild"
  ],
  "author": "Joshua Brewster",
  "license": "AGPL-3.0-or-later",
  "dependencies": {
    "@capacitor-community/bluetooth-le": "~2.0.1",
    "brainsatplay-math": "~0.0.25",
    "graphscript": "~0.1.41",
    "graphscript-services": "~0.1.54",
    "graphscript-services.gpu": "~0.1.54",
    "graphscript-services.storage": "~0.1.53",
    "webgl-plot-utils": "~0.3.4"
  },
  "devDependencies": {
    "@capacitor/android": "~4.1.0",
    "@capacitor/cli": "~4.1.0",
    "@capacitor/core": "~4.1.0",
    "@capacitor/ios": "~4.1.0",
    "@types/dom-serial": "~1.0.2",
    "@types/node": "~18.0.5"
  },
  "nodemonConfig": {
    "env": {
      "NODEMON": true
    },
    "ignore": [
      "dist/",
      ".temp/"
    ]
  }
}
